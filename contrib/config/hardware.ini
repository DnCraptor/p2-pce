# hardware.ini — аппаратные параметры RP2350
# Читается через inih (parse_pce_ini) в main.c ДО перенастройки клоков.
# Формат: стандартный INI ([section] / key = value).

[pce]
cpu_freq   = 504
psram_freq = 84
flash_freq = 66
volume     = 12
